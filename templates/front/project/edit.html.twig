{% extends 'base.html.twig' %}

{% block title %}Edit Project{% endblock %}

{% block page_title %}Éditer mon projet{% endblock %}

{% block body %}
    <div class="card shadow">
        <div class="card-body">
            {% include 'front/parts/_form.html.twig' with {"form": form, "btn": "Éditer!" } %}
        </div>
        <div class="card-footer d-flex justify-content-between">
            <a class="align-self-center" href="{{ path('project_user_index') }}">
                <i class="fas fa-chevron-left"></i> mes projects</a>
            <div class="text-right">
                {% include 'front/parts/_delete_form.html.twig' with {'entity_path': 'project_delete', 'entity': project} %}
            </div>
        </div>
    </div>
{% endblock %}
