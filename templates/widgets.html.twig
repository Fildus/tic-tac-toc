{% block autocomplete_widget %}
    <autocomplete id="{{ form.vars.id }}"
                  full_name="{{ form.vars.full_name }}"
                  label="{{ form.vars.attr.label ?? form.vars.name }}"
                  autocomplete_url="{{ form.vars.attr.autocomplete_url }}"
                  value="{{ form.vars.value }}"/>
{% endblock %}
